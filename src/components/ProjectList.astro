---
import { statusLabels } from '../data/projects.js';
import ProjectCard from './openvue/ProjectCard.vue';

/** `heading` controls whether each project title renders as h2 or h3. */
const { items, heading = 'h2' } = Astro.props;
---

<ul class="project-grid">
    {
        items.map((p) => (
            <li>
                {/* No client directive: the OpenVue card is rendered to HTML at
                    build time and ships no JavaScript. */}
                <ProjectCard project={p} status={statusLabels[p.status]} headingLevel={heading} />
            </li>
        ))
    }
</ul>
